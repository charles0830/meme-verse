name: TypeScript build action
on: [push, pull_request]
branches:
  - master
jobs:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - name: ts build
      run: |
        yarn install
        yarn build
      env:
        CI: true
